# Pathfinder2026 event requirements
#
# Keep this list small for repeatable robot image builds.
# Gamepad support is optional and uses the OS package python3-pygame.

numpy>=1.26,<3
pyyaml>=6.0,<7
opencv-python>=4.10,<5
pupil-apriltags>=1.0.4,<2
flask>=3.0,<4
pyserial>=3.5,<4
